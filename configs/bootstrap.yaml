lynx:
  application:
    name: user-service
    version: v1.0.0
    close_banner: false
  # Polaris-backed environments still rely on the same cmd/user explicit bootstrap
  # path; the difference is only where runtime config is sourced from.
  polaris:
    config_path: "configs/polaris.yaml"
    namespace: demo
    token: nu/0WRA4EqSR1FagrjRj0fZwPXuGlMpX+zCuWu4uMqy8xr1vRjisSbA25aAC3mtU8MeeRsKhQiDAynUR09I=
    weight: 100
    ttl: 10
    timeout: 5s
